import { create } from '@storybook/theming/create';
import logo from '../public/altrone.png';

export default create({
  base: 'light',
  brandTitle: 'Altrone UI',
  brandImage: logo,
  brandTarget: '_self',
});
