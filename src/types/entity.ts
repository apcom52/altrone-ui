export enum Size {
  small = 'small',
  medium = 'medium',
  large = 'large',
}

export enum Role {
  default = 'default',
  primary = 'primary',
  success = 'success',
  warning = 'warning',
  danger = 'danger',
}

export enum Gap {
  none = 0,
  narrow = 2,
  xsmall = 4,
  small = 6,
  medium = 8,
  large = 12,
  xlarge = 24,
  heading = 36,
}

export enum Direction {
  vertical = 'vertical',
  horizontal = 'horizontal',
}

export enum Align {
  start = 'start',
  center = 'center',
  end = 'end',
}
