export enum Size {
  small = 'small',
  medium = 'medium',
  large = 'large',
}

export enum Role {
  default = 'default',
  primary = 'primary',
  success = 'success',
  warning = 'warning',
  danger = 'danger',
}
