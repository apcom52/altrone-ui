import {Theme, ThemeConfig} from "../types";
import {DEFAULT_OFFSET} from "./Offset";

export const DEFAULT_THEME: ThemeConfig = {
  theme: Theme.light,
  accent: '#5F91F8',
  offset: DEFAULT_OFFSET
}