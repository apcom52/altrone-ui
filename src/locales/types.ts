export interface Localization {
  common: {
    clear: string;
    apply: string;
    close: string;
    cancel: string;
  };
  datePicker: {
    placeholder: string;
    today: string;
  };
  filePicker: {
    noFiles: string;
    placeholder: string;
  };
  numberInput: {
    increase: string;
    decrease: string;
  };
  passwordInput: {
    showPassword: string;
    hidePassword: string;
  };
  search: {
    placeholder: string;
  };
  pagination: {
    progress: string;
    title: string;
    action: string;
  };
  dataTable: {
    filters: string;
    filtering: string;
    addFilter: string;
  };
  photoViewer: {
    description: string;
  };
  scrollableList: {
    expand: string;
    collapse: string;
  };
}
