# Заметки по рефакторингу — components/switch

---

## Сводная таблица

| Приоритет | Файл | Проблема | Статус |
|---|---|---|---|
| 🟠 Высокий | `Switch.tsx:51` | `role="checkbox"` вместо `role="switch"` — неверная семантика для переключателя | ✅ Исправлено |
| 🟠 Высокий | `Switch.tsx:59–66` | `disabled` не передаётся во вложенный `<input>` | ✅ Исправлено |
| 🟠 Высокий | `switch.module.scss:27` | `outline: none` без `:focus-visible` — недоступен с клавиатуры | ✅ Исправлено — добавлен `outline` на `.Button` при `:focus-visible` |
| 🟠 Высокий | `Switch.tsx` | Отсутствует `ref` — нарушение CLAUDE.md (каждый компонент обязан форвардить `ref`) | ✅ Исправлено |
| 🟡 Средний | `Switch.tsx:70` | Пустой `.Label` всегда в DOM при отсутствии `children` | ✅ Исправлено — условный рендер |
| 🟡 Средний | `Switch.tsx:55–56` | `tabIndex={0}` + `onKeyDown` — оставлено намеренно: `.Input { display: none }`, без них переключатель не доступен с клавиатуры | ℹ️ Оставлено |
| 🟢 Низкий | `switch.module.scss` | `var(--gap)` без суффикса — переменная определена (8px), ложная тревога | ✅ Неактуально |
| 🟢 Низкий | `switch.module.scss` | `font-weight: 500; font-size: 14px` в `.Label` — захардкожена типографика | ⚠️ Остаётся |
| 🟢 Низкий | `switch.module.scss` | Захардкоженные `3px`, `4px`, `48px`, `24px`, `16px`, `8px`, `20px` вместо токенов | ⚠️ Остаётся |
