# Заметки по рефакторингу — components/switcher

---

## Сводная таблица

| Приоритет | Файл | Проблема | Статус |
|---|---|---|---|
| 🟠 Высокий | `Switcher.tsx:51` | `role="checkbox"` вместо `role="switch"` — неверная семантика для переключателя | ✅ Исправлено |
| 🟠 Высокий | `Switcher.tsx:59–66` | `disabled` не передаётся во вложенный `<input>` | ✅ Исправлено |
| 🟠 Высокий | `switcher.module.scss:27` | `outline: none` без `:focus-visible` — недоступен с клавиатуры | ✅ Исправлено — добавлен `outline` на `.Button` при `:focus-visible` |
| 🟠 Высокий | `Switcher.tsx` | Отсутствует `ref` — нарушение CLAUDE.md (каждый компонент обязан форвардить `ref`) | ✅ Исправлено |
| 🟡 Средний | `Switcher.tsx:70` | Пустой `.Label` всегда в DOM при отсутствии `children` | ✅ Исправлено — условный рендер |
| 🟡 Средний | `Switcher.tsx:55–56` | `tabIndex={0}` + `onKeyDown` — оставлено намеренно: `.Input { display: none }`, без них переключатель не доступен с клавиатуры | ℹ️ Оставлено |
| 🟢 Низкий | `switcher.module.scss` | `var(--gap)` без суффикса — переменная определена (8px), ложная тревога | ✅ Неактуально |
| 🟢 Низкий | `switcher.module.scss` | `font-weight: 500; font-size: 14px` в `.Label` — захардкожена типографика | ⚠️ Остаётся |
| 🟢 Низкий | `switcher.module.scss` | Захардкоженные `3px`, `4px`, `48px`, `24px`, `16px`, `8px`, `20px` вместо токенов | ⚠️ Остаётся |
